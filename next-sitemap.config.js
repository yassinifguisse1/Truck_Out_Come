/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://trackoutcome.agency', // Replace with your domain
    generateRobotsTxt: true,           // (optional) Create a `robots.txt` file
  };
  
  module.exports = config